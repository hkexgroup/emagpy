image: python:3.7-stretch

pages:
  script:
  - pip install -r requirements.txt
  - cd src
  - python test.py
  - cd ..
  - ls
  - cd doc
  - make html
  - cd ..
  - mv doc/_build/html/ public/
  - ls public
  artifacts:
    paths:
    - public
  only:
  - master
